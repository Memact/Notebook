# Notebook Model

Memact Notebook stores user-controlled memory entries.

Consent decides whether an app can use or write a kind of memory. Notebook shows what exists after that: who added it, why it exists, and what the user can do with it.

Entries default to private. Public or shareable entries require explicit user choice.
