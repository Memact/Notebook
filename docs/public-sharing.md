# Public Sharing

Wiki is private by default.

A public username page should show only entries the user explicitly made public or shareable. It must not expose private entries, rejected entries, raw source trails, API keys, or app secrets.
